# Travel Insurance Prediction

**Module 3 · Sprint 1: Supervised Machine Learning** | Turing College · Data Science Programme

Binary classification: predict whether a customer will purchase a travel insurance package.

---

## Project Goal

Given demographic and behavioural features, build a model that predicts the probability of travel insurance purchase. The model supports targeted marketing by identifying high-probability buyers and reducing wasted outreach.

**Primary metric:** ROC-AUC, robust to the ~35.7% positive rate.
**Secondary metrics:** PR-AUC, balanced accuracy, F1, recall, precision.

---

## Dataset

| Property | Value |
|---|---|
| Source | Kaggle: Travel Insurance Prediction Data |
| File | `data/raw/TravelInsurancePrediction.csv` |
| Raw rows | 1,987 |
| After deduplication | 1,249 (738 exact duplicates removed, 37.1%) |
| Features | 8 (3 continuous numeric, 5 categorical) |
| Target | `travel_insurance` (0 = No, 1 = Yes) |
| Positive rate | ~35.7% (post-deduplication) |

See [`references/data_dictionary.md`](references/data_dictionary.md) for full variable definitions.

---

## Repository Structure

```
.
├── data/
│   ├── raw/                    <- Original immutable data
│   └── processed/              <- Cleaned, deduplicated data
├── notebooks/
│   └── travel_insurance_prediction.ipynb   <- Main deliverable
├── references/
│   └── data_dictionary.md
├── reports/figures/            <- PNGs auto-generated by notebook
│   ├── eda/
│   ├── train_test_split/
│   ├── model_comparison/
│   ├── hyperparameter_tuning/
│   ├── final_evaluation/
│   └── error_analysis/
├── travel_insurance/           <- Python package
│   ├── config.py               <- Constants, seeds, feature lists, grids
│   ├── dataset.py              <- Load, audit, clean, split
│   ├── features.py             <- ColumnTransformer and Pipeline factory
│   ├── plots.py                <- All visualisation functions
│   ├── statistics.py           <- Hypothesis test wrappers
│   └── modeling/
│       ├── train.py            <- Cross-validation comparison
│       └── predict.py          <- Final evaluation helpers
├── Makefile
├── pyproject.toml
└── requirements.txt
```

---

## Notebook Flow

The notebook follows a strict leakage-safe pipeline. The test set is sealed at Section 5 and not touched until Section 12.

| Section | Title | Key content |
|---|---|---|
| 1 | Imports and Setup | Dependencies, paths, global settings |
| 2 | Problem Definition | Business framing, ML formulation, metric rationale |
| 3 | Data Loading and First Audit | Shape, dtypes, missings, outlier screen, variable glossary |
| 4 | Data Cleaning | Column renaming, duplicate impact assessment, deduplication |
| 5 | Train / Test Split | Stratified 80/20 split, CV strategy |
| 6 | Exploratory Data Analysis | Distributions, SQL and Pandas queries, bivariate analysis, correlations |
| 7 | Statistical Inference | Three hypothesis tests with confidence intervals |
| 8 | Preprocessing Strategy | Feature typing, engineering experiments, pipeline assembly, DR and FS |
| 9 | Baseline Models | Dummy classifier, logistic regression baseline |
| 10 | Model Comparison with CV | LR, Naive Bayes, KNN, SVM compared under identical 5-fold CV |
| 11 | Hyperparameter Tuning | GridSearchCV for top-2 models (SVM, KNN) |
| 12 | Final Test Evaluation | One-shot evaluation on held-out test set |
| 13 | Error Analysis and Interpretation | LR coefficients, learning curve, PR tradeoff |
| 14 | Conclusion, Limitations, and Recommendations | Findings, limitations, recommendations |

---

## Models

| Model | Role |
|---|---|
| DummyClassifier | Majority-class floor |
| Logistic Regression | Linear baseline |
| Naive Bayes | Probabilistic benchmark |
| KNN | Top-2 candidate, tuned with GridSearchCV |
| SVM (RBF) | Top-2 candidate, tuned with GridSearchCV |

---

## Results

| Model | ROC-AUC | PR-AUC | F1 | Balanced Accuracy |
|---|---|---|---|---|
| KNN (tuned) | **0.7053** | 0.6168 | 0.5333 | 0.6637 |
| SVM (tuned) | 0.6959 | **0.6296** | 0.5542 | 0.6620 |

KNN achieves the higher ROC-AUC. SVM achieves the higher PR-AUC and F1, indicating stronger performance on the minority class at the default threshold.

---

## How to Run

```bash
# 1. Install
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
pip install -e .

# 2. Launch notebook
make notebook
# or: jupyter lab notebooks/travel_insurance_prediction.ipynb
```

The notebook runs top-to-bottom. All random seeds are fixed via `RANDOM_SEED = 42`.

---

## Reproducibility

| Mechanism | Detail |
|---|---|
| Fixed seed | `RANDOM_SEED = 42` passed to all stochastic steps |
| Stratified split | Target distribution preserved in train and test |
| Leakage-safe pipeline | Preprocessor fitted on training data only |
| Cross-validation | `StratifiedKFold(n_splits=5, shuffle=True, random_state=42)` |

---

## Key Findings

- Annual income is the strongest individual predictor. Buyers earn significantly more on average (Welch t-test, p < 0.001).
- International travel experience is the strongest categorical predictor. Customers who have travelled abroad purchase at a ~40 percentage-point higher rate (two-proportion z-test, p < 0.001).
- Age shows a statistically significant but practically small difference between buyers and non-buyers.
- 738 exact duplicate rows (37.1%) were removed before splitting. Statistical tests confirmed the duplicates were not a systematically biased subset.
- SVM and KNN both outperform the LR baseline after tuning. The optimal operating threshold should be set using the PR curve and the business cost ratio between a missed sale and a wasted contact.
